[defaults]
# Отключаем проверку host key (для автоматизации)
host_key_checking = False

[ssh_connection]
# Отключаем мультиплексирование SSH (может вызывать зависания)
ssh_args = -o ControlMaster=no -o GSSAPIAuthentication=no -o ConnectTimeout=10